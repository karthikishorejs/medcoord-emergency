{
  "name": "medcoord-emergency",
  "version": "1.0.0",
  "description": "MedCoord Emergency Card – a healthcare PWA for medication tracking and emergency QR codes",
  "private": true,
  "scripts": {
    "start": "npx serve public -l 3000",
    "test": "jest --verbose",
    "build": "echo 'Static site – no build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@deepgram/sdk": "^3.9.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "vercel": "^39.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
